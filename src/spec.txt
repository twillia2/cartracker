# optional extras specific to a manufacturer
### BOSE® Surround Sound System
bose = (1 << 1)
#Burmester® High-End Surround Sound System
burmester = (1 << 2)
### Sport Chrono Package
sport_chrono = (1 << 3)
### Air Suspension
air_suspension = (1 << 4)
### Panoramic Roof
pano_roof = (1 << 5)
### ParkAssist (Front and Rear) including Reversing Camera
park_assist = (1 << 6)
### Adaptive Cruise Control incl. Porsche Active Safe
acc = (1 << 7)
### Head-Up Display
hud = (1 << 8)
### Performance Battery Plus
perf_battery = (1 << 9)
### LED Matrix Main Headlights
matrix = (1 << 10)
### LED main headlights
leds = (1 << 11)

bitwise_options = {
    ### BOSE® Surround Sound System
    'BOSE'.lower() : bose,
    #Burmester® High-End Surround Sound System
    'Burmester'.lower() : burmester,
    ### Sport Chrono Package
    'Sport Chrono'.lower(): sport_chrono,
    ### Air Suspension
    'Air Suspension'.lower() : air_suspension,
    ### Panoramic Roof
    'Panoramic'.lower() : pano_roof,
    ### ParkAssist (Front and Rear) including Reversing Camera
    'ParkAssist'.lower() : park_assist,
    ### Adaptive Cruise Control incl. Porsche Active Safe
    'Adaptive Cruise'.lower() : acc,
    ### Head-Up Display
    'Head-Up Display'.lower() : hud,
    ### Performance Battery Plus
    'Performance Battery'.lower() : perf_battery,
    ### LED Matrix Main Headlights
    'LED Matrix'.lower() : matrix,
    ### LED main headlights
    'LED main'.lower() : leds
}

internal_options = {
    # optional extras specific to a manufacturer
    ### BOSE® Surround Sound System
    bose = (1 << 1)
    #Burmester® High-End Surround Sound System
    burmester = (1 << 2)
    ### Sport Chrono Package
    sport_chrono = (1 << 3)
    ### Air Suspension
    air_suspension = (1 << 4)
    ### Panoramic Roof
    pano_roof = (1 << 5)
    ### ParkAssist (Front and Rear) including Reversing Camera
    park_assist = (1 << 6)
    ### Adaptive Cruise Control incl. Porsche Active Safe
    acc = (1 << 7)
    ### Head-Up Display
    hud = (1 << 8)
    ### Performance Battery Plus
    perf_battery = (1 << 9)
    ### LED Matrix Main Headlights
    matrix = (1 << 10)
    ### LED main headlights
    leds = (1 << 11)
}

def create_spec(new_car_options) -> int:
    ret = 0

    for option in new_car_options:
        for highlight in options:
            if highlight in option.lower():
                ret |= options[highlight]
                break

    return ret

#def create_spec(new_car_options):